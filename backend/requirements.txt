# ===================== 声入华夏 · 后端依赖 =====================
# Python 3.10+（本项目在 3.11.5 上开发验证）
# 安装：pip install -r requirements.txt
#
# 注意：除下列 Python 包外，还需一个「外部」依赖：
#   ffmpeg —— 系统命令，用于把浏览器录音(webm)转成 16k 单声道 wav
# 语音识别走云端「通义千问 ASR」（qwen-audio-3.0-asr-flash），密钥通过环境变量配置，见 README。

# ---- Web 框架 ----
fastapi>=0.115.0            # API 服务框架
uvicorn>=0.32.0             # ASGI 服务器（启动命令：uvicorn app.main:app）
pydantic>=2.10.0            # 请求体数据校验
python-multipart>=0.0.12    # 文件/表单上传解析（POST /v1/recordings 必需）

# ---- 学习流程状态机 ----
langgraph>=0.6.0            # LangGraph 状态机：选课→跟读→跟唱→分析→文化→报告
                            # （会顺带安装 langchain-core / langgraph-checkpoint 等）

# ---- 音频分析与拼音 ----
httpx>=0.27.0               # 调用通义千问 ASR（DashScope）HTTP 接口
pypinyin>=0.55.0            # 汉字转拼音（带声调）
